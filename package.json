{
  "name": "streamlit-podcast-generator",
  "version": "1.0.0",
  "scripts": {
    "dev": "python -m streamlit run app.py --server.port 8501 --server.address 0.0.0.0",
    "start": "python -m streamlit run app.py --server.port 8501 --server.address 0.0.0.0"
  }
}
