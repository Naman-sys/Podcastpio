{
  "name": "streamlit-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Use: python -m streamlit run app.py'",
    "start": "echo 'Use: python -m streamlit run app.py'",
    "build": "echo 'Python Streamlit app - no build needed'"
  }
}
